# Chrome-Redirecting-extension

##Chrome Extension for:
  When the url “google.com” is opened, it starts a countdown timer of 5 seconds and after that it redirects the current page to 'news.google.com'.
